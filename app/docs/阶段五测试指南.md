# 阶段五：管理后台功能 - 测试指南

## 📋 测试准备

### 1. 创建管理员用户

```bash
cd /Users/yuye/YeahWork/Python项目/login-km-system
source venv/bin/activate
python app/scripts/create_admin_user.py
```

默认管理员账号:
- 用户名: `admin`
- 密码: `admin123`

你也可以自定义用户名和密码:
```bash
python app/scripts/create_admin_user.py myAdmin myPassword123
```

### 2. 启动服务器

```bash
python run_app.py
```

服务器将在 `http://localhost:9999` 启动

### 3. 获取管理员 Token

首先使用管理员账号登录获取 Token:

```bash
curl -X POST http://localhost:9999/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123",
    "app_key": "wx_crawler_app",
    "device_id": "admin_device_001"
  }'
```

保存返回的 `token` 字段值,后续所有管理接口都需要使用此 Token。

---

## ✅ 任务 14：批量生成卡密接口

### 测试场景 1：生成 10 个卡密

```bash
curl -X POST http://localhost:9999/api/v1/admin/card/generate \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{
    "app_id": 1,
    "count": 10,
    "expire_time": "2027-01-01T00:00:00",
    "max_device_count": 2,
    "permissions": ["wechat", "ximalaya"],
    "remark": "测试套餐-标准版"
  }'
```

**预期结果**:
- 返回成功信息
- `cards` 数组包含 10 个卡密
- 每个卡密格式为 `XXXX-XXXX-XXXX-XXXX`

### 测试场景 2：生成带字典权限的卡密

```bash
curl -X POST http://localhost:9999/api/v1/admin/card/generate \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{
    "app_id": 1,
    "count": 5,
    "expire_time": "2027-12-31T23:59:59",
    "max_device_count": 3,
    "permissions": {
      "wechat": true,
      "ximalaya": true,
      "douyin": false
    },
    "remark": "测试套餐-高级版"
  }'
```

**预期结果**:
- 成功生成 5 个卡密
- 权限配置为字典格式

### 测试场景 3：非管理员权限测试

使用普通用户的 Token 调用生成卡密接口:

```bash
curl -X POST http://localhost:9999/api/v1/admin/card/generate \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer NORMAL_USER_TOKEN" \
  -d '{
    "app_id": 1,
    "count": 1,
    "expire_time": "2027-01-01T00:00:00",
    "max_device_count": 1,
    "permissions": ["wechat"]
  }'
```

**预期结果**:
- 返回 403 错误
- 提示 "需要管理员权限"

---

## ✅ 任务 15：管理员权限控制

### 测试场景 1：验证管理员依赖

管理员依赖已在 `app/utils/dependencies.py` 中实现,通过 `get_current_admin` 函数验证用户角色。

**测试方法**:
- 使用管理员 Token 访问任何管理接口 → 应该成功
- 使用普通用户 Token 访问任何管理接口 → 应该返回 403

### 测试场景 2：将普通用户升级为管理员

```python
# 在 Python 环境中执行
from app.db.sqlalchemy_db import get_sqlalchemy_db
from app.models.user import User, UserRole

db = get_sqlalchemy_db()
user = db.query(User).filter(User.username == "testuser").first()
user.role = UserRole.ADMIN
db.commit()
db.close()
```

然后使用该用户登录并测试管理接口。

---

## ✅ 任务 16：管理后台查询接口

### 16.1 查询所有用户

```bash
# 基础查询
curl -X GET "http://localhost:9999/api/v1/admin/users?page=1&size=10" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"

# 按状态筛选
curl -X GET "http://localhost:9999/api/v1/admin/users?status=normal&page=1&size=10" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"

# 关键词搜索
curl -X GET "http://localhost:9999/api/v1/admin/users?keyword=test&page=1&size=10" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

**预期结果**:
- 返回用户列表
- 包含用户ID、用户名、状态、角色、卡密数量等信息
- 支持分页

### 16.2 封禁/解封用户

**封禁用户**:
```bash
curl -X POST "http://localhost:9999/api/v1/admin/user/3/status?status=banned" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

**解封用户**:
```bash
curl -X POST "http://localhost:9999/api/v1/admin/user/3/status?status=normal" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

**验证**:
- 被封禁的用户无法登录
- 被封禁的用户权限校验失败
- 解封后恢复正常

### 16.3 查询所有卡密

```bash
# 基础查询
curl -X GET "http://localhost:9999/api/v1/admin/cards?page=1&size=10" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"

# 按状态筛选
curl -X GET "http://localhost:9999/api/v1/admin/cards?status=unused&page=1&size=10" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"

# 按应用筛选
curl -X GET "http://localhost:9999/api/v1/admin/cards?app_id=1&page=1&size=10" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"

# 关键词搜索
curl -X GET "http://localhost:9999/api/v1/admin/cards?keyword=A3KD&page=1&size=10" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

**预期结果**:
- 返回卡密列表
- 包含卡密ID、卡密字符串、状态、过期时间、权限配置等
- 显示绑定用户数和绑定设备数
- 支持分页和多种筛选

### 16.4 修改卡密状态

**禁用卡密**:
```bash
curl -X POST http://localhost:9999/api/v1/admin/card/1/status \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{"status": "disabled"}'
```

**启用卡密**:
```bash
curl -X POST http://localhost:9999/api/v1/admin/card/1/status \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{"status": "unused"}'
```

**验证**:
- 禁用的卡密无法绑定
- 禁用的卡密权限校验失败
- 启用后恢复正常

### 16.5 修改卡密权限(实时生效)

**修改权限为列表格式**:
```bash
curl -X POST http://localhost:9999/api/v1/admin/card/1/permissions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{
    "permissions": ["wechat", "ximalaya", "douyin"]
  }'
```

**修改权限为字典格式**:
```bash
curl -X POST http://localhost:9999/api/v1/admin/card/1/permissions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{
    "permissions": {
      "wechat": true,
      "ximalaya": true,
      "douyin": true,
      "kuaishou": false
    }
  }'
```

**验证**:
- 立即调用权限校验接口,新权限应该生效
- 之前没有的权限现在可以通过
- 被删除的权限现在无法通过

### 16.6 查询设备绑定列表

```bash
# 查询所有设备
curl -X GET "http://localhost:9999/api/v1/admin/devices?page=1&size=10" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"

# 按卡密ID筛选
curl -X GET "http://localhost:9999/api/v1/admin/devices?card_id=1&page=1&size=10" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"

# 按用户ID筛选
curl -X GET "http://localhost:9999/api/v1/admin/devices?user_id=3&page=1&size=10" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"

# 按状态筛选
curl -X GET "http://localhost:9999/api/v1/admin/devices?status=active&page=1&size=10" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

**预期结果**:
- 返回设备列表
- 包含设备ID、卡密、设备标识、绑定时间、最后活跃时间等
- 支持分页和多种筛选

### 16.7 禁用/启用设备

**禁用设备**:
```bash
curl -X POST http://localhost:9999/api/v1/admin/device/1/status \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{"status": "disabled"}'
```

**启用设备**:
```bash
curl -X POST http://localhost:9999/api/v1/admin/device/1/status \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{"status": "active"}'
```

**验证**:
- 禁用的设备权限校验失败
- 启用后恢复正常

---

## 📊 统计数据接口

### 获取统计数据

```bash
curl -X GET http://localhost:9999/api/v1/admin/statistics \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

**预期结果**:
```json
{
  "users": {
    "total": 5,
    "normal": 4,
    "banned": 1
  },
  "cards": {
    "total": 15,
    "unused": 8,
    "used": 5,
    "disabled": 2
  },
  "devices": {
    "total": 10,
    "active": 8,
    "disabled": 2
  },
  "apps": {
    "total": 2,
    "active": 2
  }
}
```

---

## 🔄 自动化测试脚本

运行完整的管理接口测试:

```bash
cd /Users/yuye/YeahWork/Python项目/login-km-system
source venv/bin/activate
python app/scripts/test_admin_api.py
```

**测试内容**:
1. 管理员登录
2. 批量生成卡密
3. 查询用户列表
4. 查询卡密列表
5. 更新卡密权限
6. 更新卡密状态
7. 查询设备列表
8. 更新设备状态
9. 获取统计数据
10. 更新用户状态

---

## 🎯 测试场景流程

### 场景 1：完整的卡密管理流程

1. **管理员生成卡密**
   ```bash
   # 生成 5 个卡密
   POST /api/v1/admin/card/generate
   ```

2. **用户绑定卡密**
   ```bash
   # 普通用户绑定刚生成的卡密
   POST /api/v1/card/bind
   ```

3. **管理员修改权限**
   ```bash
   # 动态增加/删除权限
   POST /api/v1/admin/card/{card_id}/permissions
   ```

4. **用户测试新权限**
   ```bash
   # 权限校验应该立即生效
   POST /api/v1/permission/check
   ```

### 场景 2：用户封禁流程

1. **查看用户列表**
   ```bash
   GET /api/v1/admin/users
   ```

2. **封禁违规用户**
   ```bash
   POST /api/v1/admin/user/{user_id}/status?status=banned
   ```

3. **被封禁用户尝试登录**
   ```bash
   # 应该返回错误：用户已被封禁
   POST /api/v1/auth/login
   ```

4. **被封禁用户权限校验**
   ```bash
   # 应该失败
   POST /api/v1/permission/check
   ```

### 场景 3：设备管理流程

1. **查看设备列表**
   ```bash
   GET /api/v1/admin/devices?card_id=1
   ```

2. **禁用异常设备**
   ```bash
   POST /api/v1/admin/device/{device_id}/status
   ```

3. **被禁用设备权限校验**
   ```bash
   # 应该失败
   POST /api/v1/permission/check
   ```

---

## ⚠️ 注意事项

1. **管理员权限**
   - 所有管理接口都需要管理员权限
   - 普通用户访问会返回 403 错误

2. **实时生效**
   - 卡密权限修改立即生效
   - 用户/卡密/设备状态修改立即生效
   - 无需重启服务器

3. **数据安全**
   - 管理员操作都有日志记录
   - 敏感操作需要验证管理员 Token

4. **性能优化**
   - 列表查询支持分页
   - 大数据量时建议使用筛选条件

---

## 📝 测试检查清单

- [ ] 管理员登录成功
- [ ] 批量生成卡密(10个)
- [ ] 批量生成卡密(100个)
- [ ] 非管理员无法访问管理接口
- [ ] 查询用户列表(无筛选)
- [ ] 查询用户列表(按状态筛选)
- [ ] 查询用户列表(关键词搜索)
- [ ] 封禁用户
- [ ] 解封用户
- [ ] 查询卡密列表(无筛选)
- [ ] 查询卡密列表(按状态筛选)
- [ ] 查询卡密列表(按应用筛选)
- [ ] 查询卡密列表(关键词搜索)
- [ ] 修改卡密状态(禁用)
- [ ] 修改卡密状态(启用)
- [ ] 修改卡密权限(列表格式)
- [ ] 修改卡密权限(字典格式)
- [ ] 权限修改实时生效验证
- [ ] 查询设备列表(无筛选)
- [ ] 查询设备列表(按卡密筛选)
- [ ] 查询设备列表(按用户筛选)
- [ ] 查询设备列表(按状态筛选)
- [ ] 禁用设备
- [ ] 启用设备
- [ ] 获取统计数据
- [ ] 完整流程测试(卡密管理)
- [ ] 完整流程测试(用户封禁)
- [ ] 完整流程测试(设备管理)

---

## 🐛 常见问题

### 1. 管理员登录失败

**问题**: 使用 admin 账号登录返回 "用户名或密码错误"

**解决**:
```bash
# 重新创建管理员用户
python app/scripts/create_admin_user.py
```

### 2. 权限验证失败

**问题**: 管理员 Token 访问管理接口返回 403

**解决**:
- 检查 Token 是否过期
- 检查用户角色是否为 admin
- 重新登录获取新 Token

### 3. 卡密生成失败

**问题**: 生成卡密时返回 "应用不存在"

**解决**:
- 检查 `app_id` 是否正确
- 确认应用状态为 normal

---

## 📚 API 文档

访问 Swagger 文档查看完整的 API 接口:

```
http://localhost:9999/docs
```

在 Swagger 中可以:
- 查看所有管理接口
- 测试接口功能
- 查看请求/响应示例
- 获取接口参数说明

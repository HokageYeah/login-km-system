# 阶段三：卡密管理系统 - 测试指南

## 📋 测试前准备

### 1. 确保服务正常运行
```bash
python run_app.py
```

### 2. 插入测试卡密数据
```bash
# 连接到 MySQL 数据库
mysql -u root -p

# 执行测试数据脚本
source app/scripts/insert_test_cards.sql
```

或者直接执行：
```bash
mysql -u root -p login_km_system_dev < app/scripts/insert_test_cards.sql
```

### 3. 准备测试账户

使用默认的测试账户：
- **用户名**: testuser
- **密码**: test123456
- **应用标识**: default_app

---

## 🧪 测试流程

### 场景 1：完整的卡密绑定流程

#### 步骤 1：用户登录
```bash
curl -X POST "http://localhost:8003/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "test123456",
    "app_key": "default_app",
    "device_id": "test-device-001"
  }'
```

**预期结果**：
```json
{
  "token": "eyJhbGci...",
  "user_status": "normal",
  "has_card": false,  // 首次登录，未绑定卡密
  "username": "testuser",
  "role": "user"
}
```

保存返回的 `token` 用于后续请求。

---

#### 步骤 2：查询我的卡密（绑定前）
```bash
curl -X GET "http://localhost:8003/api/v1/card/my" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

**预期结果**：
```json
{
  "has_card": false,
  "cards": []
}
```

---

#### 步骤 3：绑定卡密
```bash
curl -X POST "http://localhost:8003/api/v1/card/bind" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -d '{
    "card_key": "A3KD-Q7LM-P2E8-W9RZ",
    "device_id": "test-device-001",
    "device_name": "我的测试电脑"
  }'
```

**预期结果**：
```json
{
  "success": true,
  "message": "卡密绑定成功",
  "card_info": {
    "card_id": 1,
    "card_key": "A3KD-Q7LM-P2E8-W9RZ",
    "expire_time": "2026-12-31T23:59:59",
    "permissions": ["wechat", "ximalaya"],
    "bind_devices": 1,
    "max_device_count": 2,
    "status": "used",
    "remark": "测试卡密-高级套餐"
  }
}
```

---

#### 步骤 4：查询我的卡密（绑定后）
```bash
curl -X GET "http://localhost:8003/api/v1/card/my" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

**预期结果**：
```json
{
  "has_card": true,
  "cards": [
    {
      "card_id": 1,
      "card_key": "A3KD-Q7LM-P2E8-W9RZ",
      "expire_time": "2026-12-31T23:59:59",
      "permissions": ["wechat", "ximalaya"],
      "bind_devices": 1,
      "max_device_count": 2,
      "status": "used",
      "remark": "测试卡密-高级套餐"
    }
  ]
}
```

---

#### 步骤 5：在第二个设备上绑定相同卡密
```bash
# 先登录第二个设备
curl -X POST "http://localhost:8003/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "test123456",
    "app_key": "default_app",
    "device_id": "test-device-002"
  }'

# 使用新的 token 绑定
curl -X POST "http://localhost:8003/api/v1/card/bind" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer NEW_TOKEN_HERE" \
  -d '{
    "card_key": "A3KD-Q7LM-P2E8-W9RZ",
    "device_id": "test-device-002",
    "device_name": "我的第二台电脑"
  }'
```

**预期结果**：绑定成功（因为最大设备数是2）

---

#### 步骤 6：尝试绑定第三个设备（应该失败）
```bash
curl -X POST "http://localhost:8003/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "test123456",
    "app_key": "default_app",
    "device_id": "test-device-003"
  }'

curl -X POST "http://localhost:8003/api/v1/card/bind" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer DEVICE3_TOKEN_HERE" \
  -d '{
    "card_key": "A3KD-Q7LM-P2E8-W9RZ",
    "device_id": "test-device-003",
    "device_name": "我的第三台电脑"
  }'
```

**预期结果**：
```json
{
  "detail": "设备数量已达上限（2个）"
}
```

---

#### 步骤 7：查询卡密详情
```bash
curl -X GET "http://localhost:8003/api/v1/card/1" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

**预期结果**：
```json
{
  "card_id": 1,
  "card_key": "A3KD-Q7LM-P2E8-W9RZ",
  "app_name": "默认应用",
  "status": "used",
  "expire_time": "2026-12-31T23:59:59",
  "max_device_count": 2,
  "permissions": ["wechat", "ximalaya"],
  "remark": "测试卡密-高级套餐",
  "devices": [
    {
      "device_id": "test-device-001",
      "device_name": "我的测试电脑",
      "bind_time": "2026-01-27T...",
      "last_active_at": "2026-01-27T...",
      "status": "active"
    },
    {
      "device_id": "test-device-002",
      "device_name": "我的第二台电脑",
      "bind_time": "2026-01-27T...",
      "last_active_at": "2026-01-27T...",
      "status": "active"
    }
  ],
  "created_at": "2026-01-27T..."
}
```

---

#### 步骤 8：解绑设备
```bash
curl -X POST "http://localhost:8003/api/v1/card/unbind-device" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -d '{
    "card_id": 1,
    "device_id": "test-device-002"
  }'
```

**预期结果**：
```json
{
  "success": true,
  "message": "设备解绑成功"
}
```

---

### 场景 2：测试卡密过期检查

```bash
# 尝试绑定已过期的卡密
curl -X POST "http://localhost:8003/api/v1/card/bind" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -d '{
    "card_key": "E2YX-WV9Z-NM3K-PQ7R",
    "device_id": "test-device-001"
  }'
```

**预期结果**：
```json
{
  "detail": "卡密已过期"
}
```

---

### 场景 3：测试卡密禁用检查

```bash
# 尝试绑定已禁用的卡密
curl -X POST "http://localhost:8003/api/v1/card/bind" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -d '{
    "card_key": "F6TH-QW8Y-MN2K-LX4P",
    "device_id": "test-device-001"
  }'
```

**预期结果**：
```json
{
  "detail": "卡密已被禁用"
}
```

---

### 场景 4：测试应用管理（管理员）

#### 步骤 1：管理员登录
```bash
curl -X POST "http://localhost:8003/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123456",
    "app_key": "default_app",
    "device_id": "admin-device-001"
  }'
```

保存管理员的 `token`

---

#### 步骤 2：查询应用列表
```bash
curl -X GET "http://localhost:8003/api/v1/app/list" \
  -H "Authorization: Bearer ADMIN_TOKEN_HERE"
```

**预期结果**：
```json
{
  "total": 1,
  "apps": [
    {
      "id": 1,
      "app_key": "default_app",
      "app_name": "默认应用",
      "status": "normal",
      "created_at": "2026-01-27T..."
    }
  ]
}
```

---

#### 步骤 3：创建新应用
```bash
curl -X POST "http://localhost:8003/api/v1/app/create" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ADMIN_TOKEN_HERE" \
  -d '{
    "app_name": "微信公众号爬虫"
  }'
```

**预期结果**：
```json
{
  "success": true,
  "message": "应用创建成功",
  "app": {
    "id": 2,
    "app_key": "wx_crawler_a8f3e9",  // 自动生成
    "app_name": "微信公众号爬虫",
    "status": "normal",
    "created_at": "2026-01-27T..."
  }
}
```

---

#### 步骤 4：禁用应用
```bash
curl -X PUT "http://localhost:8003/api/v1/app/1/status" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ADMIN_TOKEN_HERE" \
  -d '{
    "status": "disabled"
  }'
```

**预期结果**：
```json
{
  "success": true,
  "message": "应用状态更新成功",
  "app": {
    "id": 1,
    "app_key": "default_app",
    "app_name": "默认应用",
    "status": "disabled",
    "created_at": "2026-01-27T..."
  }
}
```

---

#### 步骤 5：验证应用被禁用后的效果
```bash
# 尝试登录被禁用的应用
curl -X POST "http://localhost:8003/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "test123456",
    "app_key": "default_app",
    "device_id": "test-device-001"
  }'
```

**预期结果**：
```json
{
  "detail": "应用已被禁用"
}
```

---

#### 步骤 6：重新启用应用
```bash
curl -X PUT "http://localhost:8003/api/v1/app/1/status" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ADMIN_TOKEN_HERE" \
  -d '{
    "status": "normal"
  }'
```

---

### 场景 5：测试非管理员访问应用管理接口

```bash
# 使用普通用户的 token
curl -X GET "http://localhost:8003/api/v1/app/list" \
  -H "Authorization: Bearer USER_TOKEN_HERE"
```

**预期结果**：
```json
{
  "detail": "需要管理员权限"
}
```

---

## 🎯 使用 Swagger UI 测试（推荐）

### 1. 打开 Swagger UI
访问: http://localhost:8003/docs

### 2. 测试认证流程
1. 找到 `POST /api/v1/auth/login` 接口
2. 使用测试账户登录
3. 复制返回的 token

### 3. 设置认证
1. 点击页面右上角的 🔒 "Authorize" 按钮
2. 输入 token（不需要 "Bearer " 前缀，系统会自动添加）
3. 点击 "Authorize"
4. 点击 "Close"

### 4. 测试卡密管理接口
按照以下顺序测试：

**4.1 查询我的卡密**
- 接口：`GET /api/v1/card/my`
- 预期：返回空列表（因为还没绑定）

**4.2 绑定卡密**
- 接口：`POST /api/v1/card/bind`
- 输入：
  ```json
  {
    "card_key": "A3KD-Q7LM-P2E8-W9RZ",
    "device_id": "test-device-001",
    "device_name": "我的测试电脑"
  }
  ```
- 预期：绑定成功

**4.3 再次查询我的卡密**
- 接口：`GET /api/v1/card/my`
- 预期：返回刚绑定的卡密信息

**4.4 查询卡密详情**
- 接口：`GET /api/v1/card/1`
- 预期：返回卡密详情和设备列表

**4.5 尝试重复绑定**
- 接口：`POST /api/v1/card/bind`
- 使用相同的卡密和设备
- 预期：返回错误 "该设备已绑定此卡密"

**4.6 测试设备数量限制**
- 绑定第二个设备（应该成功）
- 尝试绑定第三个设备（应该失败）

**4.7 解绑设备**
- 接口：`POST /api/v1/card/unbind-device`
- 输入：
  ```json
  {
    "card_id": 1,
    "device_id": "test-device-002"
  }
  ```
- 预期：解绑成功

### 5. 测试应用管理接口（需要管理员）

**5.1 使用管理员登录**
- 接口：`POST /api/v1/auth/login`
- 用户名：admin
- 密码：admin123456

**5.2 查看应用列表**
- 接口：`GET /api/v1/app/list`
- 预期：显示所有应用

**5.3 创建新应用**
- 接口：`POST /api/v1/app/create`
- 输入：
  ```json
  {
    "app_name": "新应用测试"
  }
  ```
- 预期：应用创建成功，自动生成 app_key

**5.4 禁用应用**
- 接口：`PUT /api/v1/app/{app_id}/status`
- 输入：
  ```json
  {
    "status": "disabled"
  }
  ```

---

## ✅ 测试检查清单

### 卡密绑定功能
- [ ] 绑定有效卡密成功
- [ ] 绑定不存在的卡密失败
- [ ] 绑定已过期的卡密失败
- [ ] 绑定已禁用的卡密失败
- [ ] 绑定已达上限的卡密失败
- [ ] 重复绑定同一设备失败

### 卡密查询功能
- [ ] 查询我的卡密返回正确数据
- [ ] 未绑定时返回空列表
- [ ] 卡密详情包含所有设备信息

### 设备解绑功能
- [ ] 解绑自己的设备成功
- [ ] 解绑不存在的设备失败
- [ ] 解绑后设备数减少
- [ ] 所有设备解绑后卡密状态变为 unused

### 应用管理功能
- [ ] 管理员可以查看应用列表
- [ ] 管理员可以创建应用
- [ ] 管理员可以禁用/启用应用
- [ ] 普通用户无法访问应用管理接口
- [ ] app_key 自动生成正确

### 权限控制
- [ ] 未登录无法访问卡密接口
- [ ] 只能查看自己的卡密
- [ ] 只能解绑自己的设备
- [ ] 管理员权限正确验证

---

## 🐛 常见问题

### Q1: 卡密绑定失败："卡密不存在"
**原因**: 测试卡密还没有插入数据库

**解决**: 
```bash
mysql -u root -p login_km_system_dev < app/scripts/insert_test_cards.sql
```

### Q2: "需要管理员权限"
**原因**: 使用普通用户token访问了管理接口

**解决**: 使用管理员账户（admin/admin123456）登录获取token

### Q3: "该设备未绑定有效卡密"
**原因**: 设备还没有绑定卡密，或卡密已过期

**解决**: 先绑定有效的卡密

### Q4: Token 过期
**原因**: Token 默认7天有效期

**解决**: 重新登录获取新的token

---

## 📊 测试数据说明

测试卡密包含多种场景：

| 卡密 | 状态 | 设备数 | 权限 | 说明 |
|------|------|--------|------|------|
| A3KD-Q7LM-P2E8-W9RZ | unused | 2 | wechat, ximalaya | 高级套餐 |
| BH4N-XY6Z-MK8P-QR5T | unused | 2 | wechat, ximalaya | 高级套餐 |
| C5PL-MN7K-WX9Y-QR3Z | unused | 1 | wechat | 基础套餐 |
| D8RH-PQ2N-TY4M-LK6W | unused | 1 | wechat | 基础套餐 |
| E2YX-WV9Z-NM3K-PQ7R | unused | 1 | wechat | 已过期 |
| F6TH-QW8Y-MN2K-LX4P | disabled | 1 | wechat | 已禁用 |
| G4NM-RZ7Y-QP5K-WX8T | unused | 3 | wechat, ximalaya, douyin | VIP套餐 |
| H9PQ-XY3M-KL6N-WZ2R | unused | 1 | wechat | 月度套餐 |
| J7KL-MN9P-QR4X-YZ6T | unused | 5 | wechat, ximalaya, douyin, kuaishou | 年度套餐 |
| K3RY-WX8Z-NM5Q-PL7T | unused | 1 | wechat | 7天体验 |

---

## 🎓 学习建议

1. **先测试基础流程**: 登录 → 绑定 → 查询 → 解绑
2. **再测试边界情况**: 过期、禁用、设备数量限制
3. **最后测试权限**: 管理员 vs 普通用户

---

**祝测试顺利！** 🎉
